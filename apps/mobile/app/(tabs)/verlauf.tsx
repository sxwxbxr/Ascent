import { Text, View } from 'react-native';

// Platzhalter — wird im M3-Training-Arbeitspaket durch den Verlauf
// (Design: design/verlauf) ersetzt.
export default function VerlaufScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-on-surface">Verlauf folgt (M3)</Text>
    </View>
  );
}
