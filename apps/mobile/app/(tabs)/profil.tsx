import { Text, View } from 'react-native';

// Platzhalter — wird im M3-Auth-Arbeitspaket durch den Profil-Screen
// (Design: design/profil) ersetzt.
export default function ProfilScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-on-surface">Profil folgt (M3)</Text>
    </View>
  );
}
