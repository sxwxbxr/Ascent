import { Text, View } from 'react-native';

// Platzhalter — wird im M3-Training-Arbeitspaket durch den Home-Screen
// (Design: design/dashboard) ersetzt.
export default function HomeScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-on-surface">Home folgt (M3)</Text>
    </View>
  );
}
