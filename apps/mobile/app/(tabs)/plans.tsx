import { Text, View } from 'react-native';

// Platzhalter — wird im M3-Pläne-Arbeitspaket durch die Planliste
// (Design: design/trainingspl_ne) ersetzt.
export default function PlansScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-on-surface">Pläne folgen (M3)</Text>
    </View>
  );
}
