import { Text, View } from 'react-native';

// Platzhalter — wird im M3-Auth-Arbeitspaket durch den echten
// Login-Screen (Design: design/login_mobile) ersetzt.
export default function LoginScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-on-surface">Login folgt (M3)</Text>
    </View>
  );
}
